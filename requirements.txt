# 运行依赖（V4 §9：**钉死版本**）。
#
# 为什么从 `>=` 改成 `==`：这个仓库跑的是一个会操作真实手机的 Agent，
# 「上周还能跑、这周复现不出来」的成本，远高于偶尔手动升级一次的成本。
# 完整依赖图（含传递依赖）锁在 `uv.lock` 里：
#
#     uv sync            # 按 uv.lock 装出一模一样的环境
#     uv lock --upgrade  # 需要升级时显式做，然后跑全量测试
#
# 测试依赖**不在这里**（审核 §9：pytest 属于 dev，不该跟着生产依赖一起装）：
#     pip install -r requirements-dev.txt
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
httpx==0.28.1
